import { ethers } from "ethers";

async function main() {
  console.log("Deploying MegaRace to Base Sepolia...");
  // Placeholder deploy
  console.log("Deploy script stub. Wire up ethers deploy here.");
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
